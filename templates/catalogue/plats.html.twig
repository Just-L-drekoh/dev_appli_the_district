{% extends 'base.html.twig' %}

{% block title %}The District : plats{% endblock %}

{% block body %}


<video  width="100%"  class="d-none d-md-flex" loop autoplay muted>
    <source src="{{asset ('img/images_the_district/plat_view.mp4')}}" type="video/mp4">
    
</video>

<div id="presentation_plat"></div>

<div class="plats row mt-5">
  {% for plat in plats %}
  <div class='card col-12 col-md-6 col-lg-4 p-0 d-flex justify-content-center p-0 m-0'>
    <img class='card-img-top' src="{{asset('/img/images_the_district/food/' ~ plat.image)}}" style="max-height: 150px; object-fit: cover;">
    <div class='card-body'>
      <h3 class="justify-content-center">{{plat.libelle}}</h3>
      <div class='button'>
      <button class='btn btn-link '>Ajouter au Panier -></button>
      </div>    
    </div>
  </div>
  {% endfor %} 
</div>





{% endblock %} 
